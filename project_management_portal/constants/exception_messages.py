# INVALID_OFFSET_LENGTH = (
#     'Offset should be greater than or equal to zero and length should be '
#     'greater than zero', 'INVALID_OFFSET_LENGTH'
# )

INVALID_USERNAME = (
    "Invalid username, try with valid username",
    "INVALID_USERNAME"
    )

INVALID_PASSWORD = (
    "Invalid password, try with valid password",
    "INVALID_PASSWORD"
    )

INVALID_WORKFLOW_TYPE = (
    "Invalid workflow_type, try with valid workflow_type",
    "INVALID_WORKFLOW_TYPE"
    )


INVALID_LIMIT_VALUE = (
    "Invalid limit value, try with valid limit value",
    "INVALID_LIMIT_VALUE"
    )


INVALID_OFFSET_VALUE = (
    "Invalid offset value, try with valid offset value",
    "INVALID_OFFSET_VALUE"
    )


INVALID_ADMIN = (
    "Invalid admin, try with valid admin",
    "INVALID_ADMIN"
    )


INVALID_PROJECT = (
    "Invalid project, try with valid project",
    "INVALID_PROJECT"
    )


INVALID_FROM_STATE = (
    "Invalid from_state, try with valid from_state",
    "INVALID_FROM_STATE"
    )


INVALID_TO_STATE = (
    "Invalid to_state, try with valid to_state",
    "INVALID_TO_STATE"
    )


INVALID_STATE = (
    "Invalid state, try with valid state",
    "INVALID_STATE"
    )

INVALID_USER = (
    "Invalid user, try with valid user",
    "INVALID_USER"
    )


INVALID_LIST_OF_USER_IDS = (
    "Invalid list of user ids, try with valid list of user ids",
    "INVALID_LIST_OF_USER_IDS"
    )


INVALID_TASK_ID = (
    "Invalid task id, try with valid task id",
    "INVALID_TASK_ID"
    )
